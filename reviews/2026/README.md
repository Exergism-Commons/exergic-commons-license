# 2026 State review

Coverage: **195 / 195 State entities**.

The first ECL-native adjudication produced 46 `R`, 64 `S`, 41 `U`, 44 `N`. Whole-State adversarial review subsequently downgraded 11 `R` findings to scoped `S`, producing the audited provisional distribution:

- `R` — **35**
- `S` — **75**
- `U` — **41**
- `N` — **44**

Scoped adversarial review remains in progress.

These are governance outcomes only. See `../../schedules/` for exact designations capable of licensing effect.