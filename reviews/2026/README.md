# 2026 State review

Coverage: **195 / 195 State entities**.

The first ECL-native adjudication produced 46 `R`, 64 `S`, 41 `U`, 44 `N`. Whole-State adversarial review subsequently downgraded 11 initial `R` findings to scoped `S`, producing the historical post-whole-State distribution `R 35 / S 75 / U 41 / N 44`.

Scoped review and later reversibility decisions changed that distribution before the `U` phase began. The full second-pass review of all 45 dossiers that entered the `U` phase is now complete.

## Current provisional governance distribution

- `R` — **36**
- `S` — **84**
- `U` — **22**
- `N` — **53**
- **Total — 195**

The canonical current machine-readable state is `../../registry/states.yml`; completed tranche/cohort files remain immutable procedural history and retain the counts correct when they were completed.

## Completed phases

- initial adjudication: 195 / 195;
- whole-State adversarial review of initial `R`: complete;
- scoped/project adversarial review of current `S`: complete through scoped tranche 10;
- full adversarial review of the 45-entry `U` phase: complete in `adversarial/under-review/full-cohort.md`.

The `U` phase produced **14 `U → S`, 9 `U → N`, 22 retained `U`, and no `U → R`**. Retained `U` now means a case has survived an explicit second-pass review as genuinely unresolved, not that it is awaiting first examination.

Detailed evidence is normalized into `../../dossiers/states/*.md`. **151 unique State dossiers** have completed detailed normalization at the 2026-08-11 evidence cutoff.

The final State-normalization phase is the **44 original `N` dossiers** that still contain only canonical metadata/first-pass links. Once those are normalized, all 195 State dossiers will be self-contained; after that come global consistency, normative and specialist legal review before any stable operative Schedule.

These are governance outcomes only. See `../../schedules/` for exact designations capable of licensing effect.
