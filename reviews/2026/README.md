# 2026 State review

Coverage: **195 / 195 State entities**.

The first ECL-native adjudication produced 46 `R`, 64 `S`, 41 `U`, 44 `N`. Whole-State, scoped, `U`-cohort and original-`N` adversarial review phases subsequently tested those findings under falsification-first, attribution and reversibility rules.

## Current provisional governance distribution

- `R` — **36**
- `S` — **84**
- `U` — **28**
- `N` — **47**
- **Total — 195**

The canonical current machine-readable state is `../../registry/states.yml`; completed tranche/cohort files remain immutable procedural history and retain the counts correct when they were completed.

## Completed phases

- initial adjudication: 195 / 195;
- whole-State adversarial review of initial `R`: complete;
- scoped/project adversarial review of `S`: complete through scoped tranche 10;
- full adversarial review of the 45-entry `U` phase: complete in `adversarial/under-review/full-cohort.md`;
- full adversarial review of the 44-entry original-`N` phase: complete in `adversarial/no-basis/full-cohort.md`.

The `U` phase produced **14 `U → S`, 9 `U → N`, 22 retained `U`, and no `U → R`**.

The original-`N` phase produced **38 retained `N`, 6 `N → U`, and no `N → S/R`**. Austria, Costa Rica, Japan, Nauru, Samoa and South Africa now sit in `U` after current developments defeated a confident no-basis conclusion without yet establishing a sufficiently specific ECL §5 prohibited project.

## State dossier completion

Detailed current evidence is normalized into `../../dossiers/states/*.md`. **All 195 / 195 State dossiers have completed detailed normalization** at the 2026-08-11 evidence cutoff and are intended to be independently auditable without reconstructing their conclusion from historical review files.

`U` now records genuine unresolved uncertainty after adversarial review; `N` records no current ECL-prohibited State/project basis under the operative §5 standard, not ethical endorsement.

The next phase is global consistency: test every surviving `S`/`R` scope against the precise prohibited-use language in `LICENSE` §5, reconcile scope vocabulary and attribution, resolve organization/project overlap, then complete normative governance and specialist legal review before any stable operative Schedule.

These are governance outcomes only. See `../../schedules/` for exact designations capable of licensing effect.
