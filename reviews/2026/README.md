# 2026 State review

Coverage: **195 / 195 State entities**.

The first ECL-native adjudication produced 46 `R`, 64 `S`, 41 `U`, 44 `N`. Whole-State adversarial review subsequently downgraded 11 initial `R` findings to scoped `S`, producing the historical post-whole-State distribution:

- `R` — **35**
- `S` — **75**
- `U` — **41**
- `N` — **44**

Scoped adversarial review then continued from that baseline. After scoped tranche 10, the **current provisional governance distribution** is:

- `R` — **36**
- `S` — **70**
- `U` — **45**
- `N` — **44**

The canonical current machine-readable state is `../../registry/states.yml`; completed tranche files remain immutable procedural history and therefore retain the counts that were correct at the time each tranche was completed.

The **current `S` cohort has completed scoped adversarial review**. Tranche 10 re-tested the remaining former whole-State `R → S` cases — Georgia, India, Singapore and South Sudan — and all four retained scoped `S` outcomes with updated attribution boundaries.

Detailed current evidence is normalized into `../../dossiers/states/*.md` for the completed `R`/`S` work so each dossier can be understood without reconstructing its conclusion from historical tranches. **110 unique State dossiers** have completed detailed normalization at the 2026-08-11 evidence cutoff.

The next review phase is the **45 current `U` dossiers**, followed by normalization/audit of the `N` cohort.

These are governance outcomes only. See `../../schedules/` for exact designations capable of licensing effect.
