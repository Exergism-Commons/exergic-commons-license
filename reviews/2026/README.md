# 2026 review records

Historical files in this directory are procedural records and remain immutable.

For current State outcomes use `../../registry/states.yml` plus the higher-precedence `../../registry/state-outcome-overrides.yml`.

For current Schedule-preparation status use the freeze and override records under `../../registry/`.

The current working license is ECL 0.2-DRAFT. No review file has licensing effect by itself.
