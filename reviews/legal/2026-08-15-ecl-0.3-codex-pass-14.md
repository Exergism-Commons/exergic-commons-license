# ECL 0.3 Codex adversarial pre-review — pass 14

Reviewed commit: `3e4cfe83448ccdccbb9086892b318c3333178406`

This is automated technical/adversarial pre-review only. It is not qualified independent legal review and does not satisfy the legal-review gate in #207.

## CODEX-0.3-025 — P2 — Bind Schedule compatibility review to the exact License artifact

**Finding.** The pass-13 compatibility gate bound every renderer-consumed frozen Schedule source by repository-relative path and SHA-256, but identified the target License only by the mutable label `ECL-0.3-DRAFT`. If the working License text changed while retaining that label, a previously `complete` compatibility record could remain valid and the renderer could continue advertising compatibility with clauses reviewed against earlier License bytes. The two Schedule workflows also did not trigger on `LICENSE` or `versions/licenses/**`.

**Disposition: resolved in candidate.**

- `registry/schedule-license-compatibility.yml` is schema version 2 and now binds the target License artifact by exact repository-relative path plus SHA-256.
- The bound target is `versions/licenses/ECL-0.3-DRAFT.md` with SHA-256 `347a39d2e0b2a0df5bbe6c8b4bb0cc97b34f4866061d6be8522fe5f5578eb50d`.
- `tools/render_schedule.py` verifies that exact frozen artifact path and digest before accepting either a `pending` or `complete` compatibility record.
- The renderer also requires the root working `LICENSE` to be byte-identical to the bound frozen target artifact, so editing only the mutable working copy invalidates the gate as well.
- A future `complete` review still must provide a non-empty review ID and bind the entire exact renderer-consumed Schedule source set by path + SHA-256. License binding is therefore additional to, not a replacement for, source binding.
- Both `Schedule integrity` and `Schedule release readiness` now trigger on changes to `LICENSE`, `versions/licenses/**`, the renderer, the compatibility regression test and registry inputs.
- `tools/test_schedule_compatibility_gate.py` attacks a stale target-License SHA and working/frozen License drift, and asserts both workflows monitor both License artifact paths.
- The generated candidate exposes the exact target License artifact path/hash in its header while compatibility remains pending.

Primary fix commits: `3cd55b05cb9770a76e9319388e34b5f82eb07893`, `baf4852e3f87cfb672a169d946c18853b416d72d`, `c64f7f8cbbc8f355dd2985ddae90c166e0aaa1d4`, `82c58cf94e4cee9c1a1945a18b34528d51f81d99`, `a91ad3f866b14984188f003cb296aae1d33c0ddb`, `df3454f222b394d92c85c3b02095b12ae86c5ed6`, and `69c3073032c4ef8a20252ecd35093b7267fdd712`.

## Pass status

Pass 14 adds one P2. Across fourteen Codex adversarial passes, the ECL 0.3 candidate has accumulated twenty-two P1 findings and three P2 findings (twenty-five findings total). Findings remain preserved after patching for qualified-review traceability.
